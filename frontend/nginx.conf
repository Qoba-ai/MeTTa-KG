server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Same behaviour as vercel.json: /docs/* served as-is, everything else
    # falls back to index.html so client-side routing works.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
